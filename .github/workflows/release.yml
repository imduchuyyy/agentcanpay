name: Release

# Publishing a release is the trigger, not pushing a tag: the release has to
# exist before anything can be attached to it. `workflow_dispatch` re-runs
# the same build against an existing tag, for when a runner fails and the
# release is already out.
on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to build and attach binaries to
        required: true

permissions:
  contents: write
  # Provenance is signed with a workflow OIDC token, and the attestation is
  # recorded against the repository.
  id-token: write
  attestations: write

env:
  TAG: ${{ github.event.release.tag_name || inputs.tag }}

jobs:
  # A binary that reports a different version than the release it hangs off
  # is worse than a missing one, so this fails before any build starts.
  check-version:
    name: Version matches the tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      - name: Compare Cargo.toml with the tag
        run: |
          crate=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          tagged=${TAG#v}
          if [ "$crate" != "$tagged" ]; then
            echo "::error::tag $TAG expects version $tagged, workspace says $crate"
            exit 1
          fi
          echo "building $crate"

  build:
    name: ${{ matrix.target }}
    needs: check-version
    runs-on: ${{ matrix.os }}
    # Every target builds on its own architecture rather than cross-
    # compiling: the rustls backend pulls in aws-lc-sys, which needs a C
    # toolchain per target and is the usual reason a cross build fails.
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
          - target: x86_64-apple-darwin
            os: macos-15-intel
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      # Pinned to the same version as rust-toolchain.toml and pr.yml; bump
      # all three together.
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.94.0
          targets: ${{ matrix.target }}

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      # --locked so the published binary is built from the dependency
      # versions that were tested, not whatever resolved today.
      - name: Build
        run: cargo build --release --locked -p agentcanpay --target ${{ matrix.target }}

      # The asset name carries no version. That is what lets an installer
      # construct a URL under `releases/latest/download/` and fetch the
      # newest build with no API call, no token and no rate limit — the
      # tag still names the release the asset hangs off.
      - name: Package
        shell: bash
        run: |
          set -euo pipefail
          name="agentcanpay-${{ matrix.target }}"
          mkdir "$name"
          cp README.md "$name/"

          if [ "${{ runner.os }}" = "Windows" ]; then
            cp "target/${{ matrix.target }}/release/agentcanpay.exe" "$name/"
            7z a -tzip "$name.zip" "$name" > /dev/null
            asset="$name.zip"
          else
            cp "target/${{ matrix.target }}/release/agentcanpay" "$name/"
            tar czf "$name.tar.gz" "$name"
            asset="$name.tar.gz"
          fi

          # A checksum published beside the binary only proves the download
          # was not corrupted: whoever could swap one could swap the other.
          # It is the fallback for hosts without `gh`; the attestation below
          # is what actually ties these bytes to this workflow.
          if command -v sha256sum > /dev/null; then
            sha256sum "$asset" > "$asset.sha256"
          else
            shasum -a 256 "$asset" > "$asset.sha256"
          fi

          echo "ASSET=$asset" >> "$GITHUB_ENV"

      - name: Attest build provenance
        id: attest
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: ${{ env.ASSET }}

      # The bundle ships as a release asset so `gh attestation verify
      # --bundle` works offline, without an API call or a logged-in gh.
      - name: Stage the attestation bundle
        shell: bash
        run: cp "${{ steps.attest.outputs.bundle-path }}" "$ASSET.sigstore.json"

      # --clobber so a re-run replaces its own asset instead of failing.
      - name: Attach to the release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$TAG" \
            "$ASSET" "$ASSET.sha256" "$ASSET.sigstore.json" --clobber
